# Notion credentials
NOTION_TOKEN=
NOTION_DATABASE_ID=

# Feed source (default: GeekNews RSS)
FEED_URL=https://news.hada.io/rss

# Optional: OpenAI for automatic summary
OPENAI_API_KEY=
# Optional: override model
# OPENAI_MODEL=gpt-4o-mini

# Optional settings
SUMMARY_LANGUAGE=ko
MAX_ITEMS=30

# Filters (optional)
# Only keep items whose title/summary contains any of these (comma-separated)
# INCLUDE_KEYWORDS=
# Exclude items if any keyword matches
# EXCLUDE_KEYWORDS=
# Keep items with at least this many points (HTML scrape only)
# MIN_POINTS=0
